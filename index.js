/**
 * @format
 */

import { AppRegistry } from 'react-native';
import PokeApp from './src/PokeApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => PokeApp);
